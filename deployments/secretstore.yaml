apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: aws-ssm
  namespace: <namespace>
spec:
  provider:
    aws:
      service: ParameterStore
      region: <region>
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets
